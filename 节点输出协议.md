# 节点输出协议

本文档记录自由画布工作流中，各节点对外输出字段的标准含义。后续新增节点或新增字段时，优先补充本文档，再同步到前端节点、后端接口和下游读取逻辑。

## 设计原则

1. 节点内部可以保留完整结构化结果，便于调试、回溯和再次编辑。
2. 节点传给下游时，应优先传递稳定字段，避免下游直接解析一整段混乱 JSON。
3. AI 模型负责生成内容和判断语义，系统负责把模型返回整理成标准节点输出。
4. 同一节点可以同时拥有“展示给用户的内容”和“传给下游的内容”，两者不一定完全相同。
5. 用户在右侧输出面板保存后的内容，应覆盖当前节点容器内的输出，并作为后续下游节点的输入来源。
6. 当前 MVP 阶段，文本生产节点优先按“纯文本节点”处理：用户看到纯文本，下游也接收纯文本；完整 JSON 只保留在内部结果中用于调试和回溯。

## 通用字段

### ok

类型：`boolean | null`

含义：节点执行状态。

- `true`：执行成功。
- `false`：执行失败。
- `null`：执行中。

### type

类型：`string`

含义：节点输出类型，用于前端判断展示方式，也用于下游判断如何读取。

示例：

- `text-production-result`
- `script-split-result`
- `video-recognition-result`
- `node-running`
- `node-error`

### savedResult

位置：前端节点对象内部字段。

含义：当前节点保存的完整结构化结果。它不是模型原生字段，而是画布系统用于持久化节点结果的容器字段。

用途：

- 节点状态判断。
- 右侧输出面板展示。
- 下游读取当前节点输出。
- 项目保存到 localStorage。

### savedOutput

位置：前端节点对象内部字段。

含义：当前节点保存的文本化输出，通常是 `savedResult` 的 JSON 字符串，也可能是用户手动编辑后的文本。

当前约定：文本生产节点的 `savedOutput` 必须是纯文本，不展示、不下传完整 JSON。

用途：

- 右侧输出面板的编辑文本。
- 兼容用户直接修改输出内容的场景。
- 兜底传递给下游。

### raw

类型：`object | string`

含义：模型或外部服务的原始返回内容。

用途：

- 调试。
- 出错时追溯模型实际返回。
- 不建议普通下游节点直接依赖。

## 建议标准字段

### handoffText

类型：`string`

来源：工作流系统自定义字段，不是 MiMo 标准字段。

含义：当前节点明确交给下游继续处理的纯净文本。

用途：

- 文本生产节点把正文传给脚本拆分节点。
- 脚本拆分节点把纯口播传给 TTS 节点。
- 内容问答节点把最终回答传给后续总结或审核节点。

读取优先级建议：

1. 优先读取上游节点的 `handoffText`。
2. 如果不存在，再读取与节点类型匹配的核心字段，例如 `cleanScript`。
3. 如果仍不存在，再从 `payload` 中提取候选文本。
4. 最后才读取完整 `savedOutput`。

### handoffPayload

类型：`object | array`

来源：工作流系统自定义字段。

含义：当前节点交给下游的结构化核心数据。

用途：

- 脚本拆分节点向素材匹配节点传递 `visualGroups`。
- 视频识别节点向文本生产节点传递画面分析摘要。
- TTS 节点向字幕节点传递音频、时长和字级字幕。

### displayOutput

类型：`string | object`

来源：工作流系统自定义字段。

含义：用于右侧输出面板展示给用户看的内容。

说明：展示内容可以比下游内容更完整，也可以包含解释、备注、执行日志摘要；但下游不应默认读取 `displayOutput`。

## 文本生产节点字段

节点类型：`text-production`

输出类型：`text-production-result`

当前 MVP 呈现规则：文本生产节点执行后，右侧输出面板只展示纯文本；传给下游的也是该纯文本。`payload`、`raw` 等结构化结果仅保存在内部。

### mode

类型：`string`

含义：文本生产模式。

可选值：

- `contentAnalysis`：内容分析。
- `contentVariation`：内容裂变。
- `contentQA`：内容问答。

### payload

类型：`object`

含义：MiMo 按 prompt 要求填充的业务结果。

不同模式下结构不同：

- 内容分析：`summary`、`keyPoints`、`structure`、`audience`、`risks`、`nextActions`。
- 内容裂变：`strategy`、`variants`、`reusablePattern`、`nextActions`。
- 内容问答：`answer`、`basis`、`unknowns`、`followUpQuestions`。

### payload.copy

类型：`string`

含义：内容裂变结果中可直接使用的正文文案。

说明：它不是通用字段，只是 `contentVariation` 的候选正文位置。后续应由系统把它整理到 `handoffText`，下游不应长期依赖这个路径。

### payload.rawText

类型：`string`

含义：当模型没有按严格 JSON 返回时，后端保留的模型原文。

用途：兜底展示和人工编辑。

### handoffText

建议新增。

含义：文本生产节点交给下游的纯文本正文。

生成规则建议：

1. `contentVariation` 优先取用户选定的某个 `variants[].copy`。
2. 未支持用户选择前，可默认取第一个 `variants[0].copy`。
3. `contentQA` 可取 `payload.answer`。
4. `contentAnalysis` 可取 `payload.summary` 加关键要点摘要。
5. 如果模型返回非严格 JSON，则取 `payload.rawText`。

## 脚本拆分节点字段

节点类型：`script-split`

输出类型：`script-split-result`

### cleanScript

类型：`string`

含义：纯净口播脚本。

要求：

- 不包含 JSON 字段名。
- 不包含 Markdown。
- 不包含分析说明、素材备注、标题序号。
- 不换行。
- 可以直接传给 TTS 节点。

### punctuationSegments

类型：`array`

含义：按逗号、句号、问号、叹号等标点切出来的最小文本小段。

字段：

- `id`：小段 ID，例如 `seg-001`。
- `order`：顺序。
- `text`：小段文本，保留结尾标点。
- `delimiter`：该小段结尾标点。

### visualGroups

类型：`array`

含义：画面内容拆分结果。每个 group 就是后续素材匹配/剪辑时的一个视频素材容器边界。

字段：

- `id`：画面容器 ID，例如 `group-001`。
- `order`：顺序。
- `segmentIds`：该画面容器包含的小段 ID。
- `text`：该画面容器对应的口播文本。
- `mergeByAi`：是否由 AI 判断合并。
- `mergeReason`：合并或不合并原因。
- `visualSubject`：建议画面主体。
- `materialHint`：建议素材需求。

### handoffText

建议新增。

含义：等同于 `cleanScript`，传给 TTS 或字幕节点。

### handoffPayload

建议新增。

含义：传给素材匹配、剪辑、审核节点的结构化拆分。

建议结构：

```json
{
  "cleanScript": "纯净口播",
  "visualGroups": []
}
```

## 视频识别节点字段

节点类型：`video-recognition`

输出类型：`video-recognition-result`

### sourceType

类型：`string`

含义：视频输入来源。

可选值：

- `local`：本地视频。
- `url`：公开视频直链。

### source

类型：`string`

含义：实际识别的视频来源，例如本地文件名或 URL。

### outputs

类型：`array`

含义：用户选择的识别类型。

可选值：

- `contentAnalysis`：内容分析。
- `scriptExtraction`：脚本提取。
- `frameAnalysis`：画面分析。

### payload

类型：`object`

含义：视频识别的结构化结果。

### handoffText

建议新增。

含义：视频识别节点给文本生产节点的摘要文本。

生成规则建议：

1. 优先汇总 `contentAnalysis`。
2. 如果用户选择脚本提取，则加入提取脚本。
3. 如果用户选择画面分析，则加入关键画面摘要。

## 字段与 MiMo 的关系

MiMo 只负责根据 prompt 返回内容。字段是否存在，取决于我们在 prompt 中如何要求，以及后端如何标准化。

当前逻辑：

1. 后端向 MiMo 发送带字段结构的 prompt。
2. MiMo 返回 `choices[0].message.content`。
3. 后端将 content 解析为 JSON。
4. 后端把解析结果保存到节点输出。
5. 工作流系统再从节点输出中整理 `handoffText`、`handoffPayload` 等标准字段。

因此：

- `payload` 通常来自 MiMo 按 prompt 填写的内容。
- `raw` 是模型原始返回。
- `handoffText`、`handoffPayload`、`displayOutput` 是工作流系统自定义字段。
- `savedResult`、`savedOutput` 是前端画布的节点持久化字段。

## 后续待补

1. 在文本生产节点后端返回中正式加入 `handoffText`。
2. 在脚本拆分节点后端返回中正式加入 `handoffText` 和 `handoffPayload`。
3. 在前端下游读取逻辑中优先读取 `handoffText` / `handoffPayload`。
4. 增加用户选择某个文本裂变版本作为下游正文的机制。
5. 新增节点时同步补充本协议。
