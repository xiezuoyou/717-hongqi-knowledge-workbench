# 种子内容裂变 Demo 搭建规划

## 1. Demo 定位

本 Demo 是一个纯前端本地演示版，用固定种子内容和固定生成结果，演示“选择种子内容 → 查看拆解 → 生成裂变内容 → 复制导出 → 记录发布效果”的完整流程。

当前阶段不接后端、不接数据库、不做登录、不做真实 AI 调用，所有数据写在前端本地文件中，交互通过 React state 和预置逻辑完成。

## 2. 核心演示路径

| 步骤 | 用户动作 | 页面效果 | Demo 目标 |
|---|---|---|---|
| 01 | 打开首页 | 看到种子内容、热点主题、指标卡和主入口 | 让用户快速理解这是“种子内容复用工具” |
| 02 | 选择种子内容 | 进入种子详情与拆解页 | 展示内容不是案例堆叠，而是可复用结构 |
| 03 | 点击生成裂变版本 | 出现平台、风格、内容形式选择，并生成结果 | 展示 AI 辅助裂变能力 |
| 04 | 编辑、复制或下载内容 | 用户可修改正文、切换标题、复制/下载 TXT | 展示结果可直接使用 |
| 05 | 回填发布数据 | 填入平台、账号、链接和互动量 | 建立种子内容与裂变发布的关系 |
| 06 | 查看效果判断 | 展示种子使用次数、互动量和高潜提示 | 展示内容可以被追踪和迭代 |

## 3. 页面规划

### 页面一：种子内容总览

目标：打开后第一眼能看到“当前有哪些可用种子内容，以及它们为什么值得用”。

| 区域 | 展示内容 | 功能效果 |
|---|---|---|
| 顶部 Hero | 标题、副标题、当前 Demo 状态、主按钮“开始选择种子” | 点击滚动或进入种子列表区域 |
| 指标卡 | 种子数量、热点数量、可复用模板数、已模拟裂变数 | 固定数字，可做轻微数字动画 |
| 流程提示 | 种子内容 → 内容拆解 → 裂变生成 → 发布回填 → 效果判断 | 强化单线流程，不做并行功能菜单 |
| 搜索筛选 | 关键词、平台、车型/主题、风险等级、内容类型 | 前端过滤本地种子数组 |
| 种子卡片 | 标题、摘要、标签、推荐理由、风险等级、可复用点 | 点击进入种子详情页 |
| 热点信息 | 当前热点标题、背景、推荐切入角度、关联种子 | 作为种子内容的语境补充 |

### 页面二：种子内容拆解

目标：让用户看到一条内容如何被拆成可复用结构。

| 区域 | 展示内容 | 功能效果 |
|---|---|---|
| 种子概览 | 标题、简介、适用平台、适用场景、风险等级 | 可切换不同种子 |
| 推荐理由 | 为什么这条内容值得复用 | 固定文案展示 |
| 脚本拆解 | 开头钩子、核心观点、结尾互动、段落结构 | 点击某段可高亮对应可复用点 |
| 画面拆解 | 分镜、画面要求、素材类型、剪辑节奏 | 展示视频内容的可制作逻辑 |
| 素材清单 | 视频片段、图片、封面字、话题标签、参考文案 | 可点击复制素材说明 |
| 风险提示 | 不建议照搬内容、需要人工确认的表达 | 风险项有醒目标识 |
| 下一步按钮 | “基于此种子生成裂变版本” | 进入生成页 |

### 页面三：裂变生成工作台

目标：这是 Demo 的视觉重点，展示“基于种子内容生成可发布版本”的能力。

| 区域 | 展示内容 | 功能效果 |
|---|---|---|
| 左栏：来源种子 | 已选种子、引用片段、可复用观点 | 点击来源，高亮右侧正文对应段落 |
| 中栏：生成结果 | 标题候选、正文编辑器、话题标签、封面文案、评论话术 | 标题可切换，正文可编辑 |
| 右栏：生成设置与检查 | 平台、内容形式、表达风格、风险检查、来源覆盖率 | 切换选项后可重新生成预置版本 |
| 生成动画 | 理解种子、组织表达、检查风险三个阶段 | setTimeout 模拟，不请求网络 |
| 重新生成 | 在预置版本 A/B 之间切换 | 结果变化但仍引用同一批种子 |
| 复制导出 | 复制正文、复制标题、下载 TXT | 成功后显示 Toast |

### 页面四：发布回填

目标：把生成内容和发布结果绑定起来，为效果判断提供基础数据。

| 区域 | 展示内容 | 功能效果 |
|---|---|---|
| 当前种子信息 | 来源种子、生成版本、目标平台 | 自动带入当前流程信息 |
| 发布信息表单 | 发布平台、账号名称、发布链接、发布时间 | 使用 React state 保存 |
| 互动数据表单 | 播放/阅读、点赞、评论、收藏、转发 | 可手动输入，默认可填模拟数据 |
| 状态反馈 | 待发布、已发布、已回填 | 提交后状态变化 |
| 提交按钮 | “保存回填并查看效果” | 进入效果判断页 |

### 页面五：效果判断

目标：展示种子内容经过裂变后的表现，让“内容优胜劣汰”逻辑可见。

| 区域 | 展示内容 | 功能效果 |
|---|---|---|
| 效果总览 | 总发布数、总互动量、平均互动、最高表现种子 | 固定数据 + 用户刚回填的数据 |
| 种子排行 | 种子标题、使用次数、总互动、平均互动、推荐状态 | 根据本地 records 计算排序 |
| 发布记录 | 平台、账号、链接、发布时间、互动数据 | 展示回填记录 |
| AI 复盘摘要 | 哪个种子值得继续放大、哪个内容结构表现好 | 固定模板根据最高分种子生成 |
| 下一轮建议 | 继续裂变、观察、归档 | 用标签形式展示 |

## 4. 本地数据规划

| 数据文件 | 内容 | 最低数量 |
|---|---|---|
| `src/data/seeds.ts` | 种子内容、摘要、标签、风险、推荐理由、拆解结构 | 8-12 条 |
| `src/data/hotspots.ts` | 热点标题、背景、切入角度、关联种子 | 3-5 条 |
| `src/data/generatedContent.ts` | 预置生成结果 A/B，包括标题、正文、标签、评论话术 | 2 套 |
| `src/data/mockRecords.ts` | 发布回填与互动数据 | 6-10 条 |
| `src/lib/mockEngine.ts` | 本地搜索、筛选、推荐、生成、数据统计 | 1 套 |

## 5. 必须可点的交互

| 编号 | 交互 | 反馈 |
|---|---|---|
| I01 | 搜索种子内容 | 列表实时过滤，显示结果数量 |
| I02 | 筛选平台/主题/风险 | 种子卡片更新 |
| I03 | 点击种子卡 | 进入详情页 |
| I04 | 点击拆解段落 | 高亮对应可复用点 |
| I05 | 点击生成 | 出现三阶段 loading |
| I06 | 切换标题候选 | 当前标题变更 |
| I07 | 编辑正文 | 字数即时更新 |
| I08 | 重新生成 | A/B 结果切换 |
| I09 | 复制正文 | Toast 显示“已复制” |
| I10 | 下载 TXT | 浏览器下载本地文件 |
| I11 | 提交发布回填 | 新记录进入效果页 |
| I12 | 点击效果页种子排行 | 回到对应种子详情 |

## 6. 视觉方向

| 项目 | 要求 |
|---|---|
| 整体气质 | 克制、现代、内容工作台感，不做传统后台表格堆叠 |
| 主色 | 深色文字 + 红旗品牌红作为重点按钮与状态色 |
| 页面关系 | 顶部使用线性流程条，不使用左侧并行导航 |
| 首页 | 有清晰 Hero、指标卡、种子卡和热点信息 |
| 详情页 | 重点展示脚本、画面、素材、风险的结构化拆解 |
| 生成页 | 三栏布局，突出来源、正文、检查之间的关系 |
| 效果页 | 使用排行卡、简表和复盘摘要，不做复杂图表 |

## 7. 开发优先级

| 优先级 | 内容 |
|---|---|
| P0 | 纯前端项目跑通；本地数据完整；五个页面可跳转 |
| P1 | 种子总览、详情拆解、裂变生成三页完成度优先 |
| P2 | 搜索筛选、loading、复制、下载、回填、Toast 可用 |
| P3 | 效果页统计、复盘摘要、种子状态判断 |
| P4 | 动效、细节 polish、空状态、错误状态 |

## 8. 验收路径

| 时间 | 操作 |
|---|---|
| 0:00-0:30 | 打开总览页，展示种子内容、热点、指标和流程 |
| 0:30-1:00 | 搜索/筛选种子，选择一条进入详情 |
| 1:00-1:40 | 查看脚本拆解、画面拆解、素材清单和风险提示 |
| 1:40-2:20 | 进入生成页，切换平台/风格，生成裂变版本 |
| 2:20-2:45 | 切换标题、编辑正文、复制或下载 |
| 2:45-3:20 | 回填发布链接和互动数据 |
| 3:20-4:00 | 查看效果判断，展示种子排行和复盘摘要 |

