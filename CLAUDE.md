## 当前在做什么（进来先读这条）

813 种子内容裂变 · **2026-08-09 热点日视图完成，热点方向改为当日具体方向**

### 核心认知（重要）

**内容方向（热点方向）**：
- 策划定方向 → Claude 生成静态文件 → 覆盖服务器
- 前端只展示，不需要 B 端界面动态生成

**种子内容生成**：
- 策划定种子 → Claude 用 `/seed/generate` 生成静态文件 → 覆盖服务器
- 前端只使用，不需要 B 端界面

**B 端 = 摄影师素材收集页面**：
- 摄影师按拍摄指南拍摄后，自主上传素材
- **往后放**，等内容确定后再做

### 已完成（2026-08-09）

**数据层**
- ✅ `directions.ts` / `seeds.ts` / `timeline.ts`（提交 `4cbe0c6`）
- ✅ 前端展示：种子卡片、内容方向（提交 `304d609`）

**后端接口（3 个服务）**
- ✅ `/seed/generate` (8794)：生成种子内容（提交 `263401c`）
- ✅ `/seed/feasibility` (8796)：可行性判断（提交 `48d494d`）
- ✅ `/seed/package` (8793)：素材包产出（提交 `48d494d`）

**前端集成（2026-08-09 晚）**
- ✅ 可行性判断集成到 `handleSeedAiSubmit` 流程
- ✅ 三种 verdict 的 UI 处理：
  - `supported` → 静默通过，直接生成
  - `degradable` → 显示调整方案，等用户确认后继续
  - `unsupported` → 显示不支持原因和建议，终止流程
- ✅ 种子内容二级页面重构：
  - 从 `public/seeds/{seedId}.json` 动态加载 manifest 数据
  - 展示：标题、热点方向、内容拆解、逻辑线、分段结构
  - 顶部栏固定悬浮（滚动时始终可见）
- ✅ AI 裂变入口改为底部悬浮按钮 + 弹出面板
  - 底部固定悬浮按钮（始终可见）
  - 点击后弹出居中面板（遮罩层 + 卡片）
  - 输入状态：面板直接变形为输入框（无嵌套边框）
  - 黑白灰配色，适配白色背景

**热点日视图（2026-08-09 深夜）**
- ✅ 替换原有的种子发布状态板块，改为 813 传播节奏 10 天日历
- ✅ 横向滚动时间线，每天一张卡片，固定高度 200px
- ✅ 每天显示：日期标签、传播焦点、热点方向标签、执行说明
- ✅ 活动日（8/13 晚）橙色渐变高亮
- ✅ 热点方向从 4 个固定分类改为每天独立的具体参与方向
  - 不再引用全局 `directions.ts` 的 4 大方向
  - `TimelineDay.directionIds: DirectionId[]` → `directions: string[]`
  - 8/11: 长春文旅打卡地推荐、城市溜达路线种草、活动搭建花絮预热、露营带娃场景
  - 8/13 晚: 袁娅维现场唱歌、G919四电机双层极限突围、智能泊车街区复杂场景、813晚会现场氛围、汉服达人开大车反差
  - 其他日期类似拆解，共 10 天每天 3-5 个具体方向

**热点方向板块重构（2026-08-09 深夜）**
- ✅ 从显示 4 个固定分类改为显示当天的具体参与方向
- ✅ 默认显示 8/11 的 4 个方向（未来会根据实际日期动态切换）
- ✅ 为每个方向补充详细内容：
  - 卡片描述（desc）：简要说明，让卡片不再单薄
  - 热点解读（explain）：背景、来源、传播目的
  - 内容方向推荐（direction）：切入角度、具体做法、平台选择
- ✅ 修复卡片选中时上浮导致边框被遮罩裁剪的问题（增加容器顶部 padding）

**manifest 只有一个源（重要，别再手写第二份）**

唯一权威源是 `src/data/seed/manifests/*.manifest.json` —— 后端 8793 / 8796 直接读它。
前端要的那份用 `pnpm seed:sync` 生成到 `public/seeds/{seedId}.json`，
**`public/seeds/` 是产出物，不要手改**。

展示字段（标题、热点方向、内容拆解 = `angle`、状态）在 `src/data/seed/seeds.ts`，
不放进 manifest。manifest 只负责 `logicalLine` + `segments`/`clips`。

现在真的有素材的只有 **seed-006**（6 段 / 8 个真实片段，文件都在盘上）。
005、007 的 `manifestPath: null`，二级页显示「素材准备中」，AI 裂变按钮置灰不可点 ——
否则点下去后端会抛「manifest 文件不存在」。

之前踩的坑：手写过 4 份 `public/seeds/*.json`，字段和 seeds.ts 重复、segments 和后端不一致、
`clips` 全空，导致前端能展示一条后端根本做不出来的种子。已删除。

### 进行中

**等待测试**
- 可行性判断的三种路径需要真实后端数据验证
- degradable 情况下的用户确认流程
- 素材包生成完整流程（8793 端口）

### 下一步

**时间线动态日期判断**
- 当前热点方向固定显示 8/11，需要根据实际日期动态切换到对应那天
- 补充其他 9 天（8/12-8/20）的详细方向内容（desc / explain / direction）

**素材包生成接口集成**
- `/seed/package` (8793) 的完整调用和错误处理
- 下载功能验证
- 生成结果的 UI 展示优化

## 前端 UI 验证：截图工作流

改 `src/styles.css` / `src/main.tsx` 的视觉效果时，用 Playwright 截图验证。
**先读全局 `~/.claude/CLAUDE.md` 的「截图验证与上下文成本」一节**，下面只记本项目的具体信息。

### 截图脚本

`shot-tmp.mjs`（临时产物，已 ignore，用完删）。要点：

- 依赖 dev server 跑在 `http://localhost:5175/`，先确认起来了
- 视口 `1600x1000` —— **偏大，新截图改成 1000x625 左右**，看布局够用且省一半以上 token
- 通过 `addInitScript` 预置 localStorage 登录态（`hongqi-auth-users` / `hongqi-auth-account` = `Admin`），
  必须在任何 app 脚本执行前注入，否则 `useState` 初始化读不到
- 切页靠点 `[aria-label="..."]`，中文 label：`种子专题页`、`知识库`、`AI模板入口`、`个人中心`
- 脚本会收集 console error 和 pageerror 一起打出来，这部分比图便宜，优先看

注意：脚本里 landing 那段注释写着 "clear auth for this one only"，但代码并没有清，
`addInitScript` 对所有页面生效。要截未登录态的 landing 得单独开一个 context。

### `_shots/` 是什么

我自己生成的验证产物，不是项目素材，已 ignore。5 张图对应：

| 文件 | 页面 | 入口 |
|---|---|---|
| `1-landing.png` | 落地页 | `/` |
| `2-seed.png` | 种子专题页 | `#ai` → 点「种子专题页」 |
| `3-library.png` | 知识库 | 点「知识库」 |
| `4-tools.png` | AI 模板入口 | 点「AI模板入口」 |
| `5-profile.png` | 个人中心 | 点「个人中心」 |

`1-landing.png` 磁盘 1.5M，是其他张的 4-5 倍（背景渐变压不动）。
磁盘体积不代表上下文成本（见下），但它是唯一需要转 JPEG q85 再读的一张。

### 为什么反复读图会拖垮会话

**机制：图片不能被压缩掉。** 文字可以摘要成一句话，图片只能整块保留或整块丢弃。
保留就一直占着位置直到会话结束，丢弃则下次确认时又得重读。
所以图片的成本随**读取次数**线性累积，且不可回收。

单张成本没有想象的大：按分辨率算，长边压到 1568px 以内后约 `宽×高/750`，
1600×1000 一张约 2000 token。**贵的是次数，不是单张体积。**
（此前这里写"一张 7–13 万 token""52 万字符"，那是把 base64 字符数当成 token 了，是错的。）

### 这个项目踩过两次的坑

- **2026-08-07** CSS 优化：5 张图读了 18 次，98 分钟触发 9 次压缩，累计等 32 分钟。
- **2026-08-08** 扁平化：明知上面这条，又读了 8 次，其中 `2-seed.png` 一张读了 3 次。

两次同一个动作：每改一轮就把图重新读一遍确认。合计 26 次读图里约 20 次是重复。
上一版规则写的是"只读改动涉及的那一两张""循环超两轮就换新会话"——
判断类的软规则，第二次照样没拦住。改成可机械执行的硬规则：

**读图三条铁律**

1. **一张图在一个会话里只允许 Read 一次。** 读完立刻把观察写成文字清单
   （元素尺寸、间距、颜色值、对比度、错位的具体位置、哪个选择器负责）。
   之后所有讨论引用这份文字。想再读同一张之前，先问自己"上次的文字结论缺了什么"——
   通常缺的是当时没写细，不是图里没有。
2. **改完不要立刻截图。** 先跑构建 + `grep` 验证改动确实落到了预期的选择器上，
   这两步几乎免费。只有"逻辑上应该对但需要肉眼确认视觉效果"时才截图。
3. **一轮最多读 1 张。** 想读第二张就说明这一轮改的面太宽了，拆成两轮。
   需要多图对比（"这 5 张里找出间距不一致的"）派 subagent，让它只回文字结论。

配套：验证顺序永远是 **构建 → console error → grep 定位 → 文字结论 → 最后才是图**。
前四步在这个项目里已经足够抓出绝大多数问题，2026-08-08 那轮的 7 处残留彩色
就是 grep 找出来的，不是看图看出来的。

## 素材库

`素材库/图片/`、`素材库/种子内容/` 下有 45 张位图（金葵花车标、717粉丝盛典等），
是项目内容素材，**不要 Read 进上下文**。需要知道有哪些文件用 `ls` / `find` 看路径就行。

## 缺外部信息就停下来问（硬规则）

代码和文件里读不出来的东西——云上资源怎么配的、某个能力是自研还是买的现成服务、
某批密钥为什么是空的、某个目录将来要放什么——**不要靠推理填空，停下来问**。

2026-08-08 这一轮连续踩了三次：把 ADP 现成能力当成自研 RAG；把「密钥是空的」
当成「这些功能从没跑通」；把北京那个 COS 桶当成没用的旧桶。三次都是从文件里
推出来的合理猜测，三次都是错的，而且每次都推出了错误的下一步计划。

判断标准：如果一个结论会改变接下来要做什么，而它的依据只是「代码里看起来像」，
那就是该问的时候。

## 这个项目是什么

两块**互相独立**的东西，共用一个前端和一套账号：

**一、813 内容裂变（核心，优先级最高）**

8 月 13 日粉丝盛典。官方定内容方向和热点，主持方在每个热点下做「种子内容」发给
各端口使用者（4S 店员工等），他们基于种子裂变出自己的内容。813 的价值是让各端口
信息拉齐，站在非官方视角告诉他们今天有哪些热点、每个怎么做、有哪些素材能用。

种子内容 = 裂变基底，结构见 `素材库/种子内容/seed-00X-*/`：

- `README.md` —— 热点方向说明、原片信息、文件夹结构、素材放置说明
- `素材包/节奏段N_xxx/` —— 按叙事节奏分段的素材（如入口开场、位置指引、节目打卡、
  休闲体验、赛车展示、关注收尾、通用补充）
- `参考截图/`、`原始视频/`

813 当天由摄影师往素材包里填真实素材。现在里面多是 `占位素材.mp4`。

给使用者的三个功能，复杂度递增：

1. **只给文案** —— 给有剪辑能力和素材的人，他们照文案自己剪
2. **给素材包 + 文案** —— 他们在本地剪映 / PR 剪
3. **AI 产出成片** —— 走 MIMO / MINIMAX 等第三方模型，最复杂

三个功能都要支持「按使用者诉求调整口播脚本」：比如「我是 4S 店员工，想强调这条信息」，
在种子的逻辑框架内变体，可带身份、可个性化强调。

**二、RAG 知识库平台（已完成，独立于裂变）**

- **问答** —— 基于腾讯云 ADP（知识引擎）。**不是自研的**，Agent 路由和 RAG 自己写太难，
  直接用现成工具。云上应用 `AppBizId 2084629648176788096`，名字「717知识库」，
  类型 `knowledge_qa`，角色设定和模型都配好了。
- **多模态检索**（图片 / 视频 / 文档）—— 基于腾讯云 **MetaInsight** 混合检索。

## 两个知识存储不是一回事（最容易搞混的点）

| | 放什么 | 谁读它 |
|---|---|---|
| **ADP 自带知识库存储** | HS6 产品课件、用户调研报告等问答语料 | ADP 问答，云上托管，仓库里看不到 |
| **`717-assets-1328484090`**（北京，私有） | 多模态检索的素材来源，687 对象 5.79 GB | MetaInsight 检索 |
| **`hongqi-813-assets-1328484090`**（广州，公开读） | 前端要展示的图片视频 | 浏览器直读 / CDN |

HS6 那些资料在 **ADP 自己的存储**里，不在北京那个桶里。北京桶是给 MetaInsight
做图片/视频/文档语义检索用的。两个都不是前端资产桶。

### 环境变量为什么必须分开（踩过）

`storage-server.mjs` 用 `COS_BUCKET` + `COS_REGION` 同时干两件事：浏览知识库桶、
以及拼 MetaInsight 的 `ci` 域名（`${appId}.ci.${COS_REGION}.myqcloud.com`）。

2026-08-08 我为了传前端图片，把 `.env` 的 `COS_BUCKET` 改成了广州资产桶——
这会让 MetaInsight 去广州找数据集（数据集在北京），图片检索直接坏掉，而且要等
凭证填进来才会暴露。已拆开：

- `COS_BUCKET` / `COS_REGION` → **北京知识桶**，storage-server 用
- `ASSET_BUCKET` / `ASSET_REGION` → **广州资产桶**，`cos:sync` 用，缺省回落到 `COS_*`

**动这两组变量之前先想清楚是哪个用途。**

## 凭证状态

密钥都不在仓库里（`.env` 已 gitignore）。`.env` 是空槽，值由用户填。

- **ADP** —— 云上应用真实存在且配好。`.env` 里的 `ADP_*` 是空的，所以本地跑不通真实问答。
- **MIMO / MINIMAX / GRSAI** —— `/ai/*` 那 17 个路由依赖它们。**这些功能之前都跑通过**，
  key 是 Codex 做产品时没暴露出来，不是没实现。拿到 key 填上即可。
- **COS** —— `cos:sync` 直接复用 tccli 的临时凭证（`~/.tccli/default.credential`），
  不用往 `.env` 抄密钥。注意那是 STS 临时凭证，带 session token 且会过期，
  上传开始报 403 就是过期了，重新登录 tccli。

不要向用户索要密钥明文，不要把 SecretId / SecretKey 打印到输出里。

## 验证的真实覆盖范围（别夸大）

`pnpm test` 的 6 个知识问答测试打在 **stub 服务**（`tests/stub-adp-server.mjs`）上，
验证的是前端逻辑：流式拼接、非 reply 类型不混入、等待态、停止不重发、500 说人话、
空态用中性灰。**它不验证真实 ADP 链路** —— 那需要 `ADP_*` 凭证。

说「RAG 问答已上线」时要带上这个限定，否则是夸大。

`pnpm regression` 是结构体检：查已上线能力的代码特征、列真实缺口、
按 `VITE_ASSET_BASE` 配置决定素材检查走 HTTP HEAD 还是查本地磁盘。

## 前端资产走 CDN

`assetUrl()` + `VITE_ASSET_BASE`（`src/main.tsx` 顶部）。配了就走 CDN，不配回落
`/assets` 本地目录，切换不用改代码。媒体文件因此不进构建产物。

`pnpm cos:sync:dry` 看计划，`--local` 复制到 `public/`（免凭证的过渡方案），
`--commit` 真上传。上传带 `Cache-Control: public, max-age=31536000, immutable`。

已知缺口：`717-demo/seed-delivery-demo.mp4` 全仓库没有源文件。它是裂变演示态的占位，
而裂变本身是写死的假数据，所以这个视频从来没被真正生成过。裂变接真之后视频应该是
跑出来的，不该预先摆一个。

`public/` 里另有约 137M Remotion 渲染产物（`seed006-real` 等），813 前端一行不读，
但全进了 157M 的 `dist/`。搬走能让产物瘦一个数量级，需先确认 Remotion 渲染时
是否依赖 HTTP 可达。活动之后再做。

## 口径：717 → 813

粉丝盛典往年 7 月 17，今年改 **8 月 13**。分三类处理，不要一把替换：

- **前台文案** —— 改成 813
- **后端标识符**（`717-hongqi-kb`、ADP 应用名「717知识库」、MetaInsight 数据集名
  `717-image-search` 等）—— **不能动**，动了要同步改后端配置
- **去年活动的素材文件名** —— **不该动**，去年确实是 717

## 已知的演示态（不是 bug，是没接完）

`handleSeedAiSubmit` 用 `window.setTimeout` + 写死文案返回 `package` / `video` /
`script` 三种结果，全仓库共 9 处模拟延迟。这是裂变的当前状态。

接真后端时注意：假结果是**瞬间**返回的，所以界面上没有「正在处理」和「失败了怎么办」
两种状态。一接真，延迟和失败立刻暴露给使用者——而这批人连 AI 都不太了解，
点一下三秒没反应等于坏了。上一轮在知识问答里已经做过一套（等待有明确提示、
报错说人话、给重试入口、空结果用中性灰而不是红色），裂变照搬同一套模式，不用重想。
