# 云部署说明

先按“最小可用上云”来，不要一口气把渲染、素材库、剪辑全塞进同一轮。

## 1. 准备环境

1. 服务器安装 `Node.js 20+` 和 `pnpm`
2. 把仓库上传到服务器
3. 复制 `.env.example` 为 `.env`
4. 填好 `MIMO_API_KEY`、`MINIMAX_API_KEY`、公网地址

## 2. 启动顺序

```bash
pnpm install
pnpm canvas:server
pnpm ai:server
pnpm render:server
```

## 3. 访问方式

- 工作台页面：`http://你的服务器IP:5174/workflow-canvas.html`
- AI 服务：`http://你的服务器IP:8790`
- 渲染服务：`http://你的服务器IP:8787`

## 4. 你现在这套的原则

- `workflow-canvas.html` 会自动读取当前域名下的 AI 地址
- 所有返回下载链接都会用 `PUBLIC_AI_BASE_URL` / `PUBLIC_RENDER_BASE_URL`
- 服务器监听已经改成 `0.0.0.0`
- 先只测文本、脚本、语音、沙盒、素材上传
- 视频渲染先放后面

## 5. 推荐的上线方式

如果你只是先测试，直接开 3 个端口就行。
如果后面要给别人用，再加 Nginx 反代到 80 / 443。

