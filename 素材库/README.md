# 素材库

这里统一存放和知识库搜索相关的素材与说明文件。

## 目录约定

- `图片/`：图片素材，含产品图、场景图、截图等
- `文档/`：文档素材，含 Word、PDF、Markdown、说明稿
- `视频/`：视频素材，含原片、剪辑片段、示例视频
- `标签MD/`：给素材绑定的标签说明文件

## 规则

1. 新素材先按类型归类，再补标签。
2. 每个素材尽量对应一个标签说明文件。
3. 标签文件优先写清楚：类型、主体、场景、可用用途、关联关键词。
4. 后续如果某一类太多，再继续细分子目录。
