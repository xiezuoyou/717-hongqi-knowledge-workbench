# 当前可用流程稳定性基线

本文档用于保护当前已经验证可用的主流程。后续做 UI、布局、交互或包装优化时，不能破坏这里列出的能力。

## 主流程

当前主流程为：

1. 种子内容拆解
2. AI 生成脚本
3. 脚本编辑与 AI 校验
4. 口播音色选择与 TTS 生成
5. 基于口播字幕时间戳生成时间线
6. 素材选择、合并、裁切、画布缩放位移
7. AI 包装方案生成
8. Remotion 渲染 MP4

## 不可破坏的功能点

- 种子页点击下一步时，必须显示 AI 生成中的明确状态，不能让用户误以为页面卡住。
- 脚本页必须允许用户手动编辑每一句脚本。
- 脚本输入框失焦后，必须触发两类 AI 校验：
  - 当前素材库是否支持这句文案。
  - 这句话是否存在病句或表达不顺。
- 脚本页必须保留单句 AI 修改窗口，用户选中某句后可以只改当前句。
- 脚本页必须保留整体 AI 修改入口。
- 修改脚本后，后续口播、时间线、素材计划、渲染结果必须失效，避免旧时间线继续被使用。
- 音色库试听必须使用固定短句：`欢迎使用红旗内容裂变平台`。
- 音色库试听必须尽量复用缓存，避免重复消耗 TTS 额度。
- 生成完整口播时必须生成一条完整音频，并拿字幕/时间戳反推意群时间线。
- 时间线必须来自真实口播音频和字幕时间戳，不能只用预估时长。
- 素材选择页必须支持意群合并。
- 合并后的素材段允许选择多个素材，但数量不能超过合并意群数量。
- 强绑定素材只能从对应类型素材中选择，例如车灯细节只能选车灯素材。
- 素材裁切必须支持源素材起点、成片占比、顺序、contain/cover、缩放、位移。
- 前端预览中的素材缩放、位移、contain/cover 必须传入渲染。
- Remotion 渲染必须读取 `materialPlan.transform`，不能忽略画布内素材大小和位置。
- 画布比例必须从种子推荐比例开始，并允许切换 `3:4`、`9:16`、`4:3`。
- Remotion 输出尺寸必须跟画布比例一致。
- 包装页必须调用 `/ai/package-video`，失败时可以本地兜底，但不能因为接口路径丢失而 404。
- 渲染 payload 必须包含完整 `workflowProject`，包括 seed、script、voicePackage、timelineBeats、materialClips、canvasRatio、packageConfig。

## 回归检查

后续每次重要修改后至少运行：

```bash
pnpm regression
```

如果涉及渲染，还要运行：

```bash
pnpm exec remotion compositions src/remotion/index.jsx
```

如果改了 AI 服务，需要重启：

```bash
pnpm ai:server
```

如果改了渲染服务，需要重启：

```bash
pnpm render:server
```

## 已知边界

- 当前素材文件还没有真正接入素材库文件，只是先用种子原片作为可视化素材源验证裁切、缩放和位移链路。
- 包装 AI 当前输出的是结构化包装配置，后续可以继续扩展更多包装组件。
- 素材上传端、素材视频理解、素材描述库仍在后续建设范围内。
