# AI 脚本拆分逻辑分享稿

更新时间：2026-07-22

## 1. 这件事真正难在哪里

口播脚本拆分看起来像是按逗号、句号切句，但真正难点不是切句，而是判断：

- 哪几句话表达的是同一个意思。
- 哪一句话需要独立画面承接。
- 哪些内容可以用通用素材覆盖。
- 哪些内容必须使用专用素材。
- 哪些短词不能被误判成一个独立画面。

如果只是机械按标点切，画面会非常碎。

如果完全让 AI 自由拆，它又可能把不该拆的内容拆开，或者把需要专用画面的内容合并掉。

所以我们的思路不是让 AI 一步生成最终剪辑，而是先给 AI 一套明确的拆分判断框架。

## 2. 整体拆分思路

AI 拆脚本分成两层：

第一层是内容大段。

它回答的是：这段脚本从表达结构上分成哪些部分。

例如：

- 钩子
- 痛点引入
- 功能介绍
- 场景体验
- 价值升华
- 收束

第二层是画面单元。

它回答的是：每一个具体画面应该承接哪一段口播。

内容大段不是素材单位，只是帮助理解脚本结构。

真正影响后续剪辑和素材匹配的是画面单元。

## 3. 为什么不能直接按标点切

中文口播里，逗号经常不是完整语义边界。

例如：

```text
红旗智驾，让你的出行从此告别焦虑。
```

如果直接按逗号切，会变成：

```text
红旗智驾，
让你的出行从此告别焦虑。
```

这就错了。

因为“红旗智驾”只是后一句的主体，不是一个完整表达。

这类句子必须作为一个整体判断：

```text
红旗智驾，让你的出行从此告别焦虑。
```

所以拆分逻辑里要先判断一句话是不是具备完整主谓宾，或者至少是不是一个完整表达。

## 4. 核心判断一：语义是否完整

AI 拆分时，不能只看标点，而要判断语义是否完整。

一个画面单元应该尽量对应一个完整表达。

例如：

```text
你只需要轻轻握住方向盘，剩下的交给红旗智驾。
```

这句话虽然有逗号，但前后表达的是同一个出行体验：

- 用户轻握方向盘。
- 智驾系统接管辅助。

它们共同表达一个完整意思：驾驶变轻松。

所以这两句可以合并成一个画面单元。

但如果一句话里有两个不同动作或两个不同功能，就不应该强行合并。

例如：

```text
前方行人突然出现，系统主动预警并刹停。
```

这里可能涉及两个画面动作：

- 行人出现。
- 系统预警/刹停。

如果后续需要精细素材，这类内容就可能要拆开。

## 5. 核心判断二：主语是否连续

很多口播脚本里，连续几句话其实围绕同一个主语展开。

如果主语一致、表达方向一致，通常可以合并。

例如：

```text
这套系统搭载了高清度激光雷达和多个摄像头，就像给车辆装上了一双全天候的眼睛。
```

它的主语都是“这套系统”，表达的都是感知能力。

这类内容可以合并为一个画面单元。

AI 在判断时要看：

- 前后句是不是同一个主体。
- 后一句是不是在补充前一句。
- 有没有出现新的动作、新的对象或新的功能点。

如果没有明显切换，就倾向于合并。

## 6. 核心判断三：视觉主体是否一致

脚本文字最终要变成画面，所以 AI 还要判断视觉主体。

即使两句话语义上相关，如果画面主体完全不同，也可能需要拆分。

例如：

```text
镜头扫过车身线条，内饰屏幕亮起导航路线。
```

这里的视觉主体不同：

- 车身外观。
- 内饰屏幕。

所以更适合拆成两个画面单元。

反过来，如果几句话都能由同一个画面主体承接，就可以合并。

例如：

```text
车辆平稳行驶在城市道路中，红旗智驾持续识别前方车流。
```

视觉主体都是车辆行驶和智驾感知，可以合并。

## 7. 核心判断四：素材是否专用

这是我们这套逻辑里非常关键的一层。

不是所有脚本都需要同样精细的素材。

我们把画面需求分成三类：

### 通用画面

表达的是情绪、氛围、价值、泛场景。

例如：

```text
让每一次出发都更安心。
出行从此更加从容。
```

这类内容可以用通用素材承接，比如车辆行驶、用户微笑、城市道路、氛围镜头。

### 方向画面

有明确方向，但没有锁死到某个具体画面。

例如：

```text
交给红旗智驾。
红旗智驾陪你开。
```

这里出现了“红旗智驾”，但它只是方向约束，不代表一定需要某个专用镜头。

所以它不应该被直接判为专用素材。

### 专用画面

出现了具体功能、参数、部件、演示动作或可验证细节。

例如：

```text
AEB 自动刹停。
主动预警。
17 寸仪表盘。
车标升降。
汝窑漆面微距。
```

这些内容如果没有对应画面，用户会明显感觉不匹配。

所以专用画面不能随便和前后内容合并，也不能用泛素材糊过去。

## 8. AI 的合并原则

AI 合并小句时，遵循几个原则：

1. 相邻内容才允许合并。
2. 同一主体、同一意思、同一视觉方向，倾向合并。
3. 短主语、短实体词不能单独成画面。
4. 出现专用功能或具体可验证画面，倾向独立。
5. 通用内容可以合并，方向内容可以合并，专用内容要谨慎合并。
6. 一个画面单元要尽量承接一个完整表达，而不是一个孤立词。

## 9. 一个典型拆分例子

原脚本：

```text
红旗智驾，让你的出行从此告别焦虑。这套系统搭载了高清度激光雷达和多个摄像头，就像给车辆装上了一双全天候的眼睛。无论是高速巡航还是城市拥堵，它都能自动保持车距、精准车道居中，甚至能自动识别红绿灯和行人。你只需要轻轻握住方向盘，剩下的交给红旗智驾。它不仅仅是一个功能，更是你可靠的副驾，让每一次出行都变得轻松、安全。
```

第一层：内容大段。

```text
钩子：红旗智驾，让你的出行从此告别焦虑。
技术与感知：这套系统搭载了高清度激光雷达和多个摄像头，就像给车辆装上了一双全天候的眼睛。
功能场景：无论是高速巡航还是城市拥堵，它都能自动保持车距、精准车道居中，甚至能自动识别红绿灯和行人。
体验表达：你只需要轻轻握住方向盘，剩下的交给红旗智驾。
价值升华：它不仅仅是一个功能，更是你可靠的副驾，让每一次出行都变得轻松、安全。
```

第二层：画面单元。

```text
画面单元 1：红旗智驾，让你的出行从此告别焦虑。
画面单元 2：这套系统搭载了高清度激光雷达和多个摄像头，就像给车辆装上了一双全天候的眼睛。
画面单元 3：无论是高速巡航还是城市拥堵，
画面单元 4：它都能自动保持车距、精准车道居中，
画面单元 5：甚至能自动识别红绿灯和行人。
画面单元 6：你只需要轻轻握住方向盘，剩下的交给红旗智驾。
画面单元 7：它不仅仅是一个功能，更是你可靠的副驾，让每一次出行都变得轻松、安全。
```

这里可以看到：

- “红旗智驾，”没有被单独拆出去，因为它不是完整表达。
- “技术感知”可以合并，因为视觉主体一致。
- “高速巡航 / 城市拥堵”和具体功能可能需要分开，因为素材方向不同。
- “轻握方向盘 + 交给智驾”可以合并，因为它表达的是同一个体验。
- “可靠副驾 + 轻松安全”可以合并，因为它是价值收束。

## 10. 人工调整为什么仍然必要

AI 可以给出一个相对稳定的初稿，但脚本和素材之间天然存在不确定性。

比如同一句话：

```text
它能自动识别红绿灯和行人。
```

如果素材库里有专门的识别演示画面，它就是专用素材。

如果素材库里没有，只能用车辆通过路口的画面，它就可能退化成方向画面。

所以最终系统必须允许用户调整：

- AI 合并得太粗，用户可以拆开。
- AI 拆得太碎，用户可以合并。
- AI 判成专用，但素材库无法支持，后续素材节点可以反向校验。

这就是为什么我们的设计不是“AI 一次性决定一切”，而是“AI 先给结构化初稿，人再低成本修正”。

## 11. 一句话总结

这套脚本拆分逻辑的本质是：

先用标点建立最小文本单位，再让 AI 根据语义完整性、主语连续性、视觉主体一致性和素材专用程度，把小句合并成可剪辑、可匹配素材的画面单元。

